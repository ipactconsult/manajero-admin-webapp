import { PurchaseRequisition } from './purchase-requisition';

describe('PurchaseRequisition', () => {
  it('should create an instance', () => {
    expect(new PurchaseRequisition()).toBeTruthy();
  });
});
