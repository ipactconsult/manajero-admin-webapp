import { RequestForQuotation } from './request-for-quotation';

describe('RequestForQuotation', () => {
  it('should create an instance', () => {
    expect(new RequestForQuotation()).toBeTruthy();
  });
});
